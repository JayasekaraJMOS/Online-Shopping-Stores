The aim 01: Create responsive product card component with hover effects
Actual prompt: Create a Vue 3 ProductCard component with Tailwind CSS that displays product image, title, price, rating, stock status, and add to cart button. Include hover effects like shadow and scale transitions.

The aim 02: Build authentication store using Pinia for JWT token management
Actual prompt: Create a Pinia store for authentication that handles login with DummyJSON /auth/login endpoint, stores JWT token in localStorage, manages user state, and provides login/logout actions with error handling.

The aim 03: Create dark mode toggle with Tailwind dark modifier
Actual prompt: Build a Pinia theme store that toggles dark mode, stores preference in localStorage, applies dark: classes to all components, and respects system color scheme preference.

The aim 04: Design professional login page with demo accounts
Actual prompt: Create a Vue 3 LoginView component with a modern design, login form, error messages, and quick-select buttons for demo accounts (atuny0 and hbingley1).

The aim 05: Add dark mode support to all existing views
Actual prompt: Update HomeView, ProductDetailView, and CartView to support dark mode by adding dark: prefixed Tailwind classes for backgrounds, text, borders, and buttons.

The aim 06: Implement shopping cart with persistence
Actual prompt: Create a Pinia cart store that adds/removes products, calculates totals, persists to localStorage, and provides getters for count and total price.

The aim 07: Build professional navigation bar with auth and theme controls
Actual prompt: Update NavBar component to show user name when logged in, add login/logout buttons, theme toggle button with sun/moon icons, and style with gradient background.

The aim 08: Create shopping cart view page
Actual prompt: Build a CartView component that displays all cart items, shows item details with images, implements remove button, calculates order summary with tax, and provides checkout action.

The aim 09: Setup TypeScript interfaces for API responses
Actual prompt: Create Product and Review interfaces in TypeScript that match DummyJSON API response structure with optional fields for rating, stock, discount, sku, brand, and reviews.

The aim 10: Configure Tailwind CSS with dark mode
Actual prompt: Update tailwind.config.js to enable class-based dark mode and configure content paths for Vue components.

The aim 11: Update route configuration with cart and login pages
Actual prompt: Modify Vue Router to add routes for /login, /cart pages and set dynamic page titles using route meta.

The aim 12: Enhance CSS with Tailwind directives and custom scrollbar
Actual prompt: Add @tailwind directives to main CSS file, implement custom scrollbar styling, add smooth scrolling, and improve typography.

The aim 13: Fix TypeScript errors and restore color logo
Actual prompt: Fix the -webkit-mask-mode TypeScript error in NavBar.vue and restore the OMAX logo to its full-color PNG version instead of a solid mask.

The aim 14: Implement staggered grid animations and persistent auth initialization
Actual prompt: Add animation delay utility classes to style.css, apply staggered delays to product cards in HomeView, and ensure all stores (auth, theme, currency) are initialized in App.vue onMounted hook to ensure persistence and 100% type safety.

The aim 15: Implement Sort By filter in Navigation Bar
Actual prompt: Add a compact, icon-based 'Sort By' dropdown to the left of the search bar in NavBar.vue. Update the search store to handle sort state and implement reactive sorting (price, rating, newest) in HomeView.vue using a computed property.

The aim 16: Enhance Omaxy Chatbot with memory and inventory context
Actual prompt: Update chatbot.ts to use model.startChat() for multi-turn conversations. Implement RAG-lite by fetching relevant products before AI response and injecting them into the prompt for accuracy. Add localStorage persistence to chat messages.

The aim 17: Implement automatic coupon application
Actual prompt: Update HomeView.vue to automatically call cart.applyCoupon() when a coupon code is copied/claimed, ensuring a seamless rewards experience.
